datacube = {
  title = "DataCube"
  folder = "../web"
  http-port = 8888
  widgets = {
    path = "widgets"
  }
  queryCache = {
    enabled = true
#  	batchSize = 10
#	  closeIteratorTimeout = 60000
#	  controllerTickInterval = 5000
	  cubeInvalidateInterval = 600000
  }

  debug = false

}
