include "jsb.store.conf"

datacube = {
  folder = "../web"
  http-port = 8888
  widgets = {
    path = "widgets"
  }
  queryCache = {
    enabled = false
#  	batchSize = 10
#	closeIteratorTimeout = 60000
  }

  debug = false
  
}

workspace.managers.datacube = {
	folderName = ".datacube"
}