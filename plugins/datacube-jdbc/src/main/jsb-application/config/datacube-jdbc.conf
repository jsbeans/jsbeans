datacubejdbc = {
}

# задает максимальное число строк в скачиваемой удаленно выборке
datacube.query.engine.loopback.limit = 5000
datacube.query.engine.loopback.postgresql.api.maxsize = 5000000
datacube.query.engine.loopback.clickhouse.api.maxsize = 5000000